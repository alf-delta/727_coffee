# Design Tokens — Coffee Ops / Monoblend

Извлечено из фактического использования в `apps/web/src/styles.css` (~9700 строк, 1 файл на весь UI, без Tailwind/токен-системы — значения захардкожены inline). Это снимок текущего состояния, а не аспирационная система: значения сгруппированы по частоте и визуальной роли, чтобы ими можно было пользоваться как источником правды при дальнейшей разработке.

---

## 1. Цвет

Тёплая «кофейная» палитра: кремовый фон, тёмно-оливковые чернила вместо чёрного, золотой акцент, терракотовый для алертов.

### 1.1 Нейтральные — фон / поверхности (светлые, тёплый крем)

| Токен | Hex | Использование |
|---|---|---|
| `--color-surface-cream` | `#f4f0dd` | фон, часть градиента `:root` |
| `--color-surface-mist` | `#eef0ed` | фон, часть градиента `:root` |
| `--color-surface-gold-tint` | `#ffefad` | фон, часть градиента `:root` |
| `--color-surface-card` | `#f2f2ee` | карточки/панели |
| `--color-surface-white` | `#ffffff` | карточки, инпуты (32 использования) |

Базовый градиент фона (`:root`, [styles.css:7-9](apps/web/src/styles.css#L7-L9)):
```css
background:
  radial-gradient(circle at 88% 12%, rgb(255 216 79 / 72%), transparent 32%),
  linear-gradient(115deg, #eef0ed 0%, #f4f0dd 58%, #ffefad 100%);
```

### 1.2 Нейтральные — текст / чернила (тёмные, тёплый оливково-чёрный)

| Токен | Hex | Использование |
|---|---|---|
| `--color-ink-900` | `#20211d` | основной текст (22) |
| `--color-ink-800` | `#242421` | основной текст, самый частый тёмный (35) |
| `--color-ink-700` | `#24251f` | текст/бордеры (24) |
| `--color-ink-600` | `#34342f` | вторичный текст |
| `--color-ink-500` | `#40423c` | вторичный текст |
| `--color-ink-solid` | `#11120f` | `color` в `:root`, самый тёмный |
| `--color-ink-950-alt` | `#17181a` / `#171814` | почти-чёрные акценты |

### 1.3 Нейтральные — приглушённый текст (тёплый серый)

| Токен | Hex | Использование |
|---|---|---|
| `--color-muted-500` | `#77796f` | подписи, мета-текст (24) |
| `--color-muted-500-alt` | `#77776f` | подписи (12) |
| `--color-muted-400` | `#555851` / `#555952` | вторичный текст (18/8) |
| `--color-muted-300` | `#5c5f57` / `#6c6e65` | плейсхолдеры |
| `--color-muted-200` | `#8f8d83` / `#8b8d83` | disabled/hint |
| `--color-muted-100` | `#666960` | иконки/бордеры |

### 1.4 Брендовый акцент — золото

| Токен | Hex | Использование |
|---|---|---|
| `--color-accent-gold` | `#ffd84f` | основной акцент, CTA, хайлайты (19) |
| `--color-accent-gold-deep` | `#f2bd26` | hover/active состояние золота |

### 1.5 Семантические цвета

| Токен | Hex | Роль |
|---|---|---|
| `--color-danger` | `#8f3520` | ошибки/деструктивные действия (12) |
| `--color-danger-strong` | `#9b4136` | ошибки, более насыщенный (8) |
| `--color-danger-soft` | `#7d3f38` / `#b5482e` | ошибки, мягкий вариант |
| `--color-danger-tint` | `#f6e0da` | фон плашки ошибки |
| `--color-success` | `#1d684a` | успех/подтверждение (6) |
| `--color-success-soft` | `#5b8c5a` | успех, мягкий вариант |
| `--color-success-tint` | `#dbeadf` | фон плашки успеха |
| `--color-warning` | `#ff7a1a` | предупреждение/alert (4) |

### 1.6 Прозрачные оверлеи (стекломорфизм)

Используются поверх и тёмного, и светлого — это основа «glass» эффектов панелей:

```css
/* светлый оверлей поверх тёмного / бликов на кнопках */
--overlay-white-08: rgb(255 255 255 / 8%);
--overlay-white-36: rgb(255 255 255 / 36%);
--overlay-white-46: rgb(255 255 255 / 46%);
--overlay-white-48: rgb(255 255 255 / 48%);   /* самый частый (22) */
--overlay-white-55: rgb(255 255 255 / 55%);
--overlay-white-58: rgb(255 255 255 / 58%);   /* второй по частоте (16) */
--overlay-white-72: rgb(255 255 255 / 72%);

/* тёмный оверлей поверх светлого — тени/бордеры на кремовом фоне */
--overlay-ink-07: rgb(36 36 33 / 7%);
--overlay-ink-08: rgb(24 25 22 / 8%);
--overlay-ink-09: rgb(24 25 22 / 9%);
--overlay-ink-10: rgb(24 25 22 / 10%);        /* самый частый тёмный оверлей (15) */
--overlay-ink-12: rgb(24 25 22 / 12%);
```

---

## 2. Типографика

**Шрифт:** `Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif` (задан в `:root`, единственное семейство на весь проект).
**Моноширинный** (для кодов/цифр): `ui-monospace, "SF Mono", SFMono-Regular, Menlo, Consolas, monospace`.

### 2.1 Шкала размеров (по частоте использования)

| Токен | px | Роль |
|---|---|---|
| `--font-size-2xs` | `9px` | самые мелкие лейблы |
| `--font-size-xs` | `10px` | мета/подписи |
| `--font-size-sm` | `11px` | вторичный текст |
| `--font-size-base` | `12px` | базовый UI-текст (самый частый, 75×) |
| `--font-size-md` | `13px` | базовый текст (67×) |
| `--font-size-lg` | `14px` | акцентный текст (22×) |
| `--font-size-xl` | `15px` | подзаголовки |
| `--font-size-2xl` | `16px` | заголовки блоков |
| `--font-size-3xl` | `18px` | заголовки секций |
| `--font-size-4xl` | `20px` | крупные заголовки (14×) |
| `--font-size-5xl` | `24px` | заголовки страниц (9×) |
| `--font-size-6xl` | `26px`–`30px` | герой-заголовки |
| `--font-size-hero` | `clamp(24px, 3vw, 34px)` … `clamp(32px, 3.4vw, 46px)` | адаптивные hero-заголовки (4 вариации clamp) |

Промежуточные значения (`12.5px`, `13.5px`, `11.5px`) встречаются — похоже на ручную подгонку под конкретные строки, не часть системной шкалы.

### 2.2 Начертания

| Токен | Значение | Использование |
|---|---|---|
| `--font-weight-regular` | `400` | редко, базовый текст |
| `--font-weight-medium` | `500` | вторичные акценты |
| `--font-weight-semibold` | `560` / `600` | заголовки, кнопки (600 — самый частый, 34×) |
| `--font-weight-bold` | `620` / `650` | сильные акценты |
| `--font-weight-heavy` | `700` / `750` | заголовки (700 — 24×) |
| `--font-weight-black` | `800` / `900` | hero-заголовки, брендовые акценты (800 — 24×) |

Нестандартные промежуточные веса (`520`, `560`, `620`, `640`, `650`, `750`) — вариативный шрифт Inter используется с точной настройкой, не только с шагом 100.

### 2.3 Трекинг (letter-spacing)

Применяется почти исключительно к капслок-лейблам/эйбрам:
`0.02em`, `0.03em`, `0.04em`, `0.05em`, `0.06em`, `0.08em`, `0.1em`, `0.12em` — положительный трекинг для лейблов; `-0.035em` — отрицательный для крупных заголовков.

---

## 3. Радиусы скругления

| Токен | px | Роль |
|---|---|---|
| `--radius-xs` | `8px`–`9px` | мелкие элементы (чипы, бейджи) |
| `--radius-sm` | `10px` | инпуты, мелкие кнопки |
| `--radius-md` | `12px` | стандартные кнопки/поля (14×) |
| `--radius-lg` | `13px`–`14px` | карточки среднего размера |
| `--radius-xl` | `16px` | карточки (22×) |
| `--radius-2xl` | `18px` | крупные карточки/панели (25×, самый частый после pill) |
| `--radius-3xl` | `20px`–`24px` | модалки, крупные поверхности |
| `--radius-pill` | `999px` | пилюли/бейджи/круглые кнопки (65×, самый частый радиус вообще) |
| `--radius-circle` | `50%` | аватарки, круглые иконки |

Составные радиусы (`0 0 18px 18px`, `6px 18px 18px 18px` и т.п.) встречаются точечно — угловые скругления для «пришитых» к карточке элементов (табы, хедеры), не отдельный токен.

---

## 4. Тени

| Токен | Значение | Роль |
|---|---|---|
| `--shadow-sm` | `0 6px 14px rgb(45 42 31 / 7%)` | лёгкий подъём (инпуты, чипы) |
| `--shadow-md` | `0 8px 18px rgb(45 42 31 / 8-9%), inset 0 1px 0 rgb(255 255 255 / 76-78%)` | карточки со стеклянным бликом сверху (самый частый паттерн) |
| `--shadow-lg` | `0 9px 20px rgb(20 20 18 / 18%), inset 0 1px 0 rgb(255 255 255 / 11%)` | приподнятые панели на тёмном |
| `--shadow-xl` | `0 18px 44px rgb(83 78 54 / 10%)` | модалки/поповеры (самая частая крупная тень, 5×) |
| `--shadow-danger` | `0 8px 18px rgb(155 65 54 / 18%)` | приподнятые деструктивные кнопки |

Паттерн: внешняя тень тёплого тона (`rgb(45 42 31 / …)` или `rgb(83 78 54 / …)`) + `inset 0 1px 0 rgb(255 255 255 / 70-80%)` для стеклянного блика сверху — это основа карточного стиля во всём проекте.

---

## 5. Стекломорфизм (blur)

| Токен | Значение |
|---|---|
| `--blur-xs` | `4px` |
| `--blur-sm` | `7px` |
| `--blur-md` | `16px` (самый частый, 6×) |
| `--blur-lg` | `18px` |
| `--blur-lg-saturated` | `blur(18px) saturate(1.08)` |

---

## 6. Отступы и gap

Явной 4/8-px сетки нет, но фактическое распределение группируется вокруг чисел, кратных ~2px:

| Токен | px | Частота |
|---|---|---|
| `--space-1` | `2px` | |
| `--space-2` | `4px` | |
| `--space-3` | `6px` | |
| `--space-3-5` | `7px` | |
| `--space-4` | `8px` | самый частый gap (86×) |
| `--space-5` | `9px`–`10px` | |
| `--space-6` | `12px` | |
| `--space-7` | `14px` | |
| `--space-8` | `16px` | |
| `--space-9` | `18px` | |
| `--space-10` | `20px` | |

Составные значения (`10px 12px`, `8px 10px`, `12px 14px`) — паттерн «вертикальный меньше горизонтального» для padding кнопок/полей, не отдельные токены.

Адаптивные dashboard-токены (уже объявлены как CSS-переменные в `:root`, [styles.css:2-5](apps/web/src/styles.css#L2-L5)):
```css
--dashboard-gap: clamp(12px, 1.15vw, 18px);
--dashboard-card-gap: clamp(12px, 1vw, 16px);
--dashboard-card-height: clamp(200px, 30dvh, 320px);
```

---

## 7. Переходы (motion)

| Токен | Значение |
|---|---|
| `--duration-fast` | `120ms` |
| `--duration-base` | `150ms` |
| `--duration-base-alt` | `160ms` (самая частая, 21×) |
| `--duration-slow` | `180ms` |

Везде уважается `@media (prefers-reduced-motion: reduce)` (3 отдельных блока).

---

## 8. Брейкпоинты

Не по строгой шкале (значения подобраны под конкретную вёрстку), но фактические точки:

| px | Использование |
|---|---|
| `390` | самый маленький мобильный |
| `560`, `620`, `640` | компактный мобильный |
| `720` | основной мобильный брейкпоинт (6 использований — главная граница desktop/mobile) |
| `721` | парный min-width к 720 |
| `760`, `900`, `980` | планшет/малый десктоп |
| `1100`, `1180`, `1181` | десктоп |
| `1360`, `1470` | широкий десктоп |

`--bp-mobile: 720px` — де-факто главная граница, остальное — точечные корректировки под конкретные компоненты.

---

## Как этим пользоваться

Токены нигде не объявлены как настоящие CSS custom properties (кроме 4 в `:root`) — все значения инлайновые. Эта таблица — обратный инжиниринг фактически используемой палитры/шкал, чтобы:
1. новые компоненты подбирали значения **из существующего набора**, а не изобретали смежные оттенки/радиусы;
2. при будущем рефакторинге в реальные CSS-переменные (`--color-*`, `--space-*` и т.д.) можно было взять эту таблицу как ТЗ.
