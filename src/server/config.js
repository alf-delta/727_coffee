export const UID_COOKIE = 'mb_uid';

export const MAX_ATTEMPTS_PER_DAY = 3;
export const REWARDED_ATTEMPTS_PER_DAY = 1;
export const REWARD_TOKEN_TTL_MINUTES = 20;

export const GAMES = {
  flappy: { label: 'Flying Syrnik' },
  tap: { label: 'Tap Pressure' },
};
