{
  "name": "monoblend-tree",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "REWARD_TOKEN_SECRET=dev-only-secret-never-use-in-production COUPON_STAFF_PIN=2468 vite",
    "build": "vite build && node scripts/check-performance-budgets.js",
    "preview": "vite preview",
    "test": "node --test test/*.test.js",
    "smoke": "node scripts/smoke.js",
    "verify": "npm test && npm run build && npm run smoke"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "@vercel/analytics": "^2.0.1",
    "@vercel/edge": "^1.3.1",
    "lucide": "^1.28.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
