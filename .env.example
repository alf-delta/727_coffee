# Use long, unique values in production.
REWARD_TOKEN_SECRET=
COUPON_STAFF_PIN=

# Upstash Redis (required in production for shared, durable coupon state).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
